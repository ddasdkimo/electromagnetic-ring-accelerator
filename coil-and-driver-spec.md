# 電磁環形加速器 — 單級線圈估算 + 驅動電路規格

> 由 my_car(XIAO ESP32-S3 專案工作區)協力估算。老闆已核可。
> 對應圖文頁:`~/code/2026/my_car/docs/emring-driver.html`(SVG 電路圖 + 表格)

## 前提假設
- 漆包線:0.5mm 裸銅徑(截面 A=π×0.25²=0.196 mm²,銅 ρ=1.72×10⁻⁸ Ω·m,≈0.088 Ω/m)
- 線圈:內徑 15.5mm(隧道外徑)、繞 8 層、約 280–300 匝(取 290 匝 ≈ 36 匝/層)、軸向長 ≈18mm
- 供電:12V;功率級 IRLB8721 + SS54;12V/5A + 5A 保險絲
- 下列為**空管(無鋼球)**估算;鋼球進入孔內時電感顯著上升(可 2–3 倍)

## 1. 線圈電氣參數

| 項目 | 估值 | 公式 / 說明 |
|---|---|---|
| 各層平均半徑 | 8.0 → 11.5 mm(均值 9.5mm) | 首層 15.5/2+0.25;每層 +0.5mm |
| 用線總長 | **≈ 17 m**(280–300 匝:16.7–17.9m) | Σ匝 × 2π·平均半徑 |
| 線圈直流電阻 | **≈ 1.5 Ω**(1.45–1.6) | R = ρL/A ≈ 0.088Ω/m × 17m |
| 迴路總電阻 | ≈ 1.55–1.65 Ω | +IRLB8721 Rds(on)(3.3V 閘極 ~0.016–0.025Ω)+佈線 |
| 電感(空管) | **≈ 0.9–1.1 mH** | Wheeler 多層式 ≈0.92mH;鋼球置中升 2–3× |
| **L/R 時間常數 τ** | **≈ 0.65 ms** | τ = L/R(空管) |
| 穩態電流(理論) | ≈ 7.7 A | 12V / 1.55Ω(需持續導通才達到) |
| **實際脈衝峰值** | **≈ 6–7 A** | 脈寬 1–1.5ms:I=I_max(1−e^(−t/τ)),1ms→6.1A、1.5ms→6.9A |
| 每脈衝磁能 | ½LI² ≈ 0.02 J | — |
| 每脈衝電阻熱 | I²R·t,隨脈寬(低速可達 1–2J) | 低工作週期整體散熱無虞 |

### ⚠️ 關鍵發現:12V/5A 供電偏緊
脈衝峰值 6–7A 超過 5A 額定 → 供電下垂、加速力道打折。
**對策:12V 端並電容組 2200–4700µF(低 ESR),放線圈附近**,由電容供給脈衝峰值、PSU 只補平均值(標準 coilgun 做法)。

### ✅ 元件裕度
- IRLB8721:連續 62A;Vgs(th) 1.35–2.35V,**3.3V 閘極可直驅**(比 IRLZ44N 適合 logic-level,不選 IRLZ44N 正確)。
- SS54(40V/5A 蕭特基):續流 6–7A 短脈衝,平均 5A、突波上百 A,綽綽有餘。

## 2. 建議單級脈衝寬度 vs 球速(0.5–3 m/s)

幾何近似 **w ≈ d_eff / v**,取有效加速段 d_eff ≈ 10mm(進入強力區到線圈中心)。
實務**提早 ~1–1.5ms(≈2τ)關斷**,讓電流尾流在球到中心前衰減、避免 suck-back。

| 球速 v (m/s) | 幾何脈寬 w=d_eff/v | 建議設定(扣 ~1ms 尾流) | 電流狀態 |
|---|---|---|---|
| 0.5 | ≈ 20 ms | ~19 ms | 飽和(~7.7A,注意單發熱) |
| 1.0 | ≈ 10 ms | ~9 ms | 飽和 |
| 1.5 | ≈ 6.7 ms | ~5.7 ms | 飽和 |
| 2.0 | ≈ 5.0 ms | ~4.0 ms | 飽和 |
| 2.5 | ≈ 4.0 ms | ~3.0 ms | 接近飽和 |
| 3.0 | ≈ 3.3 ms | ~2.3 ms | 接近飽和(仍 >電流建立所需) |

**重點結論:** 0.5–3 m/s 全落在「電流飽和區」(τ≈0.65ms,~3ms 就到穩態,而最短脈寬 3.3ms 仍大於電流建立時間)→ 每級電流都能充分建立、力道接近上限;**限制因素是幾何與時序,而非電流上升**。這對 suck-back 調校是好消息:專心調關斷時機即可。

## 3. 單級驅動電路(文字描述)

940nm IR 對射(光電晶體)→ XIAO ESP32-S3 GPIO → IRLB8721(100Ω 串聯 + 10k 下拉)→ 線圈 + SS54 飛輪 → 12V/5A + 5A 保險絲

```
        +12V ────┬──────────────┬───────────── (T5A 慢熔保險絲在此上游)
                 │              │
            [電容組]        線圈上端
          2200–4700µF          │
             (低ESR)         ▓▓▓▓▓  加速線圈 L≈1mH / R≈1.5Ω
                 │           線圈下端 = 節點 D
                 │              ├──────┐
                 │            [SS54]   │        SS54:陰極(帶環端)朝 +12V
                 │          陰極↑陽極↓  │              陽極朝 D。方向接反會短路燒毀。
                 │              │      │
                 │              D──────┘
                 │              │
                 │          [IRLB8721]  低邊開關
                 │           G   D   S
                 │           │       │
   ESP32 GPIO_OUT│──[100Ω]───┤       │
                 │           │       │
                 │         [10k]      │   閘極對地 10k 下拉
                 │           │        │
        GND ─────┴───────────┴────────┴─────────

   IR 發射: 3V3 ─[150–220Ω]─▷│(940nm LED)─ GND      (~15–20mA)
   IR 接收: 940nm 光電晶體, 集極→3V3, 射極→節點→[10k 下拉]→GND, 節點→ESP32 GPIO_IN
            ★ 接收輸出務必落在 0–3.3V,絕不可拉到 12V(會燒 ESP32)
```

### 接法重點
1. **低邊開關**:線圈上端固定 +12V,下端接汲極 D,源極 S 接 GND。
2. **SS54 飛輪**跨接線圈:陰極朝 +12V、陽極朝 D。關斷瞬間吸收線圈反電動勢。**接反=短路燒毀**。
3. **閘極**:GPIO_OUT → 100Ω 串聯 → IRLB8721 閘極;閘極對 GND 加 10k 下拉(開機/浮接確保關斷)。100Ω 開關較慢但振鈴/EMI 小,對單發脈衝夠用。
4. **IR 對射(940nm)**:發射端限流 150–220Ω;接收端輸出加 10k 拉,訊號 0–3.3V,**勿拉 12V**。
5. **保險絲**:12V 端串 **5A 慢熔(T5A)**,**電容組放保險絲之後、線圈之前** → 保險絲只看平均電流(遠小於 5A),脈衝峰值由電容供給,不會誤跳。**用快熔會被 6–7A 脈衝跳掉。**
6. **供電/接地**:ESP32 用自己的 USB 供電,**不要**用 12V 直供;但兩者**必須共地**。12V 大脈衝走粗線、星狀接地,ESP32 訊號地獨立回線,避免地彈跳誤觸發 IR。

## 4. Suck-back 時機 + 測速(ESP32 軟體)
- **Suck-back(吸回)**:鋼球過線圈中心後若還通電,磁力會把球往回拉減速。線圈必須在球**到中心之前**關斷;關斷後電流靠 τ(≈0.65ms)衰減,殘流也會吸,故要**提早關**。
- **時序**:IR 偵測球進入 → 延遲 d → 開 MOSFET 脈寬 w → 關。d、w 依球速逐級微調(這正是用 MCU 的價值)。
- **測速**:同一級用**兩道 940nm IR 對射(已知間距 s)**,量遮斷時間差 Δt → v = s/Δt。ESP32 即時算 v、預測到達中心時間、動態設定下一級 d/w,並記錄各級末速做效率評估。

## 5. 4 級系統延伸(對應你定案:4 路 IR 輸入 + 4 路 MOSFET 輸出)
- 每級一組上述電路;4 顆 MOSFET 閘極接 4 支獨立 GPIO。
- 4 路 IR 輸入建議用**帶硬體比較器/施密特**的模組(或軟體去彈),避免遮斷邊緣抖動誤觸。
- 共用 12V + 電容組;每級 MOSFET 汲極各自的 SS54。
- 一顆球通過:IR1 觸發→算速→開線圈1→...→IR4;逐級末速遞增即驗證加速有效。
```
